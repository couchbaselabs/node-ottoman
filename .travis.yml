language: node_js
node_js:
  - "4.4"
  - "5.1"
  - "8.10.0"

# 0.6 is not enabled as couchnode does not support
#   this particular version of Node.js.
# 0.8 is not enabled as many of our imported libraries
#   do not support it.
